# 修改操作日志 — 记一笔

**当前版本:** v2.5.3
**版本历史:** v2.4.0 → v2.5.0 → v2.5.1 → v2.5.2 → v2.5.3

---

## v2.5.3 (2025) — Bug 修复 & 功能调整

### 修复：DeepSeek 收据拍照识别请求超时

**问题:** `callOcrVisionAPI` 使用 `fetch` 无超时设置，API 无响应时请求永久挂起，表现为一直「上传中」。

**修复:** 
- 新增 `AbortController` 45 秒超时，超时后提示「请求超时，请检查网络或图片大小」
- DeepSeek 默认模型已改为 `deepseek-v4-flash`（支持 Vision）

### 优化：备注框常开

**改动:** 交易弹窗的备注输入框不再折叠，始终可见。拍照/附件按钮保持原位。

### 优化：手机端交易弹窗增加左右边距

**改动:** 手机端弹窗左右增加 12px 边距，不再贴边。

### 调整：AI 分析移入统计页

**改动:**
- 侧栏（桌面）和底部导航（手机）移除独立的「AI 分析」Tab
- AI 分析作为统计页的子 Tab 存在（概览 / 消费类型 / 预算 / AI 分析）
- 功能和操作逻辑不变

---

## v2.5.2 (2025) — Bug 修复 & 优化

### 修复：DeepSeek 收据拍照识别失败

**问题:** DeepSeek 默认使用 `deepseek-chat` 模型不支持图片输入，导致拍照识别收据功能报错。

**原因:** `deepseek-chat` 是纯文本模型，不具备视觉能力。

**修复:** 
- `OCR_PROVIDER_DEFAULTS` 中 DeepSeek 默认模型改为 `deepseek-v4-flash`（支持 Vision 的多模态模型）
- DeepSeek V4-Flash 价格低廉（$0.14/$0.28 per M tokens），适合 OCR 场景

### 调整：AI 配置入口移至设置页

**问题:** AI 服务配置同时存在于登录界面和设置页，入口重复且容易混淆。

**改动:**
- 登录界面齿轮面板中的「收据 OCR 识别」配置区块已移除
- AI 服务配置统一在「设置」页 →「AI 服务配置」卡片中管理
- 提示文字已更新，说明可在记账时拍照识别收据

### 优化：交易弹窗桌面端增加圆角边距

**改动:**
- 桌面端交易弹窗增加 `border-radius: 16px` 圆角
- 弹窗四周增加 `1.5rem` 边距，不再紧贴屏幕边缘
- 手机端保持全屏沉浸式设计不变

---

## v2.5.1 (2025) — Bug 修复

### 修复：新增支出弹窗金额栏不吸底

**问题:** 新增/编辑交易弹窗中，金额输入框和保存按钮没有贴在弹窗底部，在部分手机上被内容顶起。

**原因:** `.trans-amount-bar` 缺少 `margin-top: auto`，无法在 flex 布局中被推到容器底部。

**修复:** 
- `.trans-amount-bar` 新增 `margin-top: auto`
- 转账表单使用同一 class，自动同步修复

---

## v2.5.0 — 主要更新

### 1. AI 配置全面迁移至 DeepSeek

**动机:** 统一使用 DeepSeek API，替代原有的 Anthropic Claude + OpenAI 多供应商方案。

### 改动详情

**HTML 登录设置:**
- 服务商选择下拉 `#ocr-provider-select`：
  - 移除 `Anthropic Claude` 选项
  - 新增 `DeepSeek（推荐）` 设为默认首选项
  - 保留 `OpenAI` 和 `自定义（OpenAI 兼容接口）` 作为备选

**`OCR_PROVIDER_DEFAULTS` 常量:**
- 默认提供商从 `anthropic` → `deepseek`
- `deepseek`: `endpoint = 'https://api.deepseek.com/v1/chat/completions'`, `model = 'deepseek-chat'`

**`onOcrProviderChange()`:**
- 默认回退从 `anthropic` → `deepseek`

**`initOcrKeyUI()`:**
- 未配置时默认选用 `'deepseek'`

**`callOcrVisionAPI()`:**
- 移除 Anthropic 专属的 `/v1/messages` 调用分支（含 `x-api-key` / `anthropic-version` / `anthropic-dangerous-direct-browser-access`）
- 所有提供商统一走 **OpenAI 兼容格式**（`chat/completions`）
- DeepSeek / OpenAI 使用各自内置 endpoint；自定义提供商使用用户输入的 endpoint

**AI 分析代码 (`aiGetModelName` / `aiGetApiUrl`):**
- `aiGetModelName`: 新增 `case 'deepseek': return 'deepseek-chat'`, 默认回退 `deepseek-chat`
- `aiGetApiUrl`: 新增 `case 'deepseek': return 'https://api.deepseek.com/v1/chat/completions'`, 默认回退 DeepSeek

> **注意:** DeepSeek 不支援 Vision/图片识别，收据 OCR 扫描功能在 DeepSeek 下不可用。
> 如需 OCR 功能请切换至 OpenAI 或兼容服务商。

---

## 2. 修复「AI 设置」按钮无反应

**问题:** AI 分析页右上角的齿轮按钮 `#ai-settings-btn` 的 `onclick` 属性使用转义引号，导致点击无效。

**修复:**
- 删除错误的 inline `onclick="switchTab('settings', document.querySelector('[onclick*=\\'settings\\']'))"`
- 新增独立函数 `goToAiSettings()`：
  - 查找设置导航按钮并触发点击
  - 展开登录设置面板（`toggleLoginSettings()`）
  - 设 300ms 延迟确保展开

---

## 3. UI 全面重新设计 (Mobile + Desktop)

### Desktop 端改动

| 区域 | 改动 |
|------|------|
| **侧栏** | 激活项添加阴影 `box-shadow` + 渐变指示条（左竖线），hover 态半透明背景，整体间距优化 |
| **侧栏 Avatar** | 使用渐变色 `linear-gradient(135deg, #0d6efd, #6f42c1)` + 微阴影 |
| **按钮** | 统一切换为 `border-radius: 10px`，按下 `scale(0.96)` 缩放反馈 |
| **卡片** | 增加圆角 `14px`，微阴影 + hover 抬起效果 |
| **登录界面** | 背景径向渐变点缀，圆角 `20px`，大阴影 + backdrop-filter 毛玻璃效果 |
| **整体动效** | `cubic-bezier(0.4, 0, 0.2, 1)` 过渡曲线，更自然的 iOS 风格 |
| **字体** | `-apple-system, BlinkMacSystemFont, "Noto Sans SC"` 优先级 |

### Mobile 端改动

| 区域 | 改动 |
|------|------|
| **底部 Tab Bar** | 毛玻璃效果 `backdrop-filter: blur(16px)`，半透明背景，顶部阴影 |
| **Tab 激活态** | 不填充背景色，改为蓝色文字 + 顶部 3px 蓝色指示条 |
| **FAB 按钮** | 渐变背景 `linear-gradient(135deg, #0d6efd, #6f42c1)`，更大阴影 |
| **页面切换** | 手机端使用由下至上的滑入动画 `translateY(10px) → 0` |
| **交易卡片** | 增加圆角和边框，更清晰的视觉分隔 |

---

## 修改的文件

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `index.html` | 修改 | AI 配置 HTML + CSS 全面重设计 + AI 分析设置修复 + JS DeepSeek 适配 |
| `manifest.json` | 未改 | - |
| `sw.js` | 未改 | - |
| `修改操作日志_v2.5.0.md` | **新建** | 本文件 |
